# Copy this file to ".env" and fill in a value.
# This password is used ONLY by your local SQL Server container.
# Rules: at least 8 characters, with uppercase, lowercase, and a number or symbol.
MSSQL_SA_PASSWORD=